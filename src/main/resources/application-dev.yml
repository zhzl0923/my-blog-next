spring:
  datasource:
    url: jdbc:mysql://localhost:3306/next_blog?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      connection-timeout: 1000
      idle-timeout: 60000
      max-lifetime: 10